<div id="<%= dom_id category %>">
  <p>
    <%= category.name %>
    <%= link_to t('.edit'), edit_category_path(category) %>
    <%= button_to t('.delete'), category_path(category), method: :delete, data: { turbo_confirm: t('common.confirm') } %>
  </p>

</div>
