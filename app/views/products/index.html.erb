<h1>Productos:</h1>

<!-- 
 Refactor the code in the index.html.erb file to use the partial product.html.erb.
<% @products.each do |product| %>
 
    <%= link_to product_path(product), class: "product" do %>
        <%= image_tag product.photo, width: 100 if product.photo.attached? %>
        <h2><%= product.title %></h2>
        <p><%= product.price %>$</p>
    <% end %>

<% end %> 

-->

<%= render partial: 'product', collection: @products %>