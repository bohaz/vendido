<h1>Productos:</h1>

<% @products.each do |product| %>
 
    <%= link_to product_path(product.id) do %>
    <h2><%= product.title %></h2>
    <p><%= product.description %></p>
    <p><%= product.price %>$</p>
    <% end %>

<% end %>